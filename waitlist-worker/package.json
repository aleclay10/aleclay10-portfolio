{
  "name": "aleclay10-waitlist-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "schema:local": "wrangler d1 execute aleclay10-waitlist --local --file=schema.sql",
    "schema:remote": "wrangler d1 execute aleclay10-waitlist --remote --file=schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260820.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.125.0"
  }
}
